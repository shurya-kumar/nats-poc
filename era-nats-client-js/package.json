{
  "name": "era-nats-client",
  "version": "1.0.0",
  "description": "This is used to call API's of the internal ERA Service and send data",
  "main": "src/index.ts",
  "scripts": {
    "build": "npx tsc",
    "start": "npx tsc && node build/index.js",
    "start-ec2": "NODE_OPTIONS=--tls-max-v1.3 npx tsc && node build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Shurya Kumar N S",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.24.0",
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "minimist": "^1.2.5",
    "nats": "^2.4.0"
  },
  "devDependencies": {
    "@types/node": "^16.11.12",
    "eslint": "^8.4.1",
    "typescript": "^4.5.3"
  }
}
